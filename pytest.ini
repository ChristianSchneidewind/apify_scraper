[pytest]
pythonpath = .
filterwarnings =
    # external dependency warning (apify/crawlee via pydantic v2 internals)
    ignore:Support for class-based `config` is deprecated, use ConfigDict instead\.:pydantic.warnings.PydanticDeprecatedSince20:pydantic\._internal\._config
